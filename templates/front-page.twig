{% extends "base.twig" %}
{% set templateClass =  template ? '--'~template  : '' %}
{% set templateSlug = template ?  '-'~template  : '' %}
{% set mainMod = "--front-page" %}
{% set hasSideBar = true %}

{% block before_head %}
  <!-- front-page.twig -->
  <!-- template - {{ template }} -->
  <!-- {{ hidePageTitle }} -->
{% endblock %}

{% block hero %}
  {% if removeHero %}
  {% else %}
    {{  parent() }}
  {% endif %}
{% endblock %}

{% block content %}

  {% for post in posts %}
    {% include ['content/content-'~templateSlug~'.twig','content/content-single-page.twig'] %}
  {% endfor %}

{% endblock %}

{% block subsidiary %}

  {{ parent() }}

{% endblock %}

{% block belowContent %}
  <!-- Removed comments from front-page.twig -->
{% endblock %}

{% block sidebar %}
  <div class="l-main__sidebar">
    <div class="c-sidebar">
      <div class="c-sidebar__widgets">
        {{ sidebar_widgets }}
      </div>
      {% if post.page_sidebar_custom_code != '' %}
        <div class="c-sidebar__page-sidebar">
          {{ fn('do_shortcode', post.page_sidebar_custom_code) }}
        </div>
      {% endif %}
    </div>
  </div>
{% endblock %}






