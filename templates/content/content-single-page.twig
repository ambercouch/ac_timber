<article id="post-{{ post.id }}" class="c-single-page{{ templateClass }} {{ hidePageTitle ? 'has-title-hidden' : '' }}">

  {% if(post.thumbnail or acSettings.defaultImage) %}
    <div class="c-single-page__feature-image{{ templateClass }} ">
      <div class="feature-image--c-single-page">
        {% if(post.thumbnail.src()) %}
          <img src="{{ post.thumbnail.src('feature1000') }}" class="feature-image--c-single-page__img" alt="{{ post.title }}"/>
        {% else %}
          <img src="{{acSettings.defaultImage}}" class="feature-image--c-single-page__img" alt="{{ post.title }}"/>
        {% endif %}
      </div>
    </div>
  {% endif %}

    <header class="entry-header c-single-page__header{{ templateClass }} {{ hidePageTitle ? 'is-hidden' : '' }}">
      {% if not hidePageTitle %}
      <h1 class="entry-title c-single-page__heading{{ templateClass }} ">
        <span class="entry-title c-single-page__title{{ templateClass }} ">{{ post.title }}</span>
      </h1>
      {% endif %}
    </header>

    <div class="entry-content c-single-page__body{{ templateClass }} ">

      <div class="c-single-page__content{{ templateClass }}">
        {{ post.content }}
      </div>

    </div>

    <footer class="c-single-page__footer{{ templateClass }} ">
    </footer>

</article>