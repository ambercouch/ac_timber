  <div class="masthead">
    <div class="masthead__branding">
      <div class="masthead__site-title">

        {%  if acSettings.svgLogo  %}
          {%  include 'inc/site-title-svg.twig' %}
        {% else %}
          {% include 'inc/site-title-text.twig' %}
        {% endif %}

      </div>
    </div>
{#<script>#}
  {#var width = (window.innerWidth > 0) ? window.innerWidth : screen.width;#}
  {#//alert(width);#}
{#</script>#}

    <button id="menuButton" class="btn--menu" data-state="off"> <span class="btn--menu__bar--tl"></span> <span class="btn--menu__bar--tr"></span> <span class="btn--menu__bar--m"></span> <span class="btn--menu__bar--br"></span> <span class="btn--menu__bar--bl"></span> </button>
    {% include 'inc/masthead-navigation.twig' %}

  </div>
