{% if fn('is_front_page') %}
  <h1 class="site-title">
    <a href="{{ site.url }} " rel="home">{{ site.title }}</title> </a>
  </h1>
{% else %}
  <span class="site-title">
               <a href="{{ site.url }} " rel="home">{{ site.title }}</title> </a>
            </span>
{% endif %}